Usage:

Requires Python 3

RepackBars.py <bars file>.bars <folder containing original files>
	<folder containing new files>
	
	Finds each of the original files inside of the .bars file, and replaces
	it with the new file of the same name. Automatically pads smaller files