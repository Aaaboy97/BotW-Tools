Usage:

Requires Python 3

RepackBars.py <bars file>.bars <folder containing original files>
	<folder containing new files>
	
	Searches the bars file for all files from the original folder, and
	replaces with padding the original with the new file of the same name